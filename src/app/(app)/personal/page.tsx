import InicioPersonalClient from './InicioPersonalClient';

export const metadata = { title: 'Personal — FinanceMe' };

export default function InicioPersonalPage() {
  return <InicioPersonalClient />;
}
